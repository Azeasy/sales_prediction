# Balanced policy: compromise between service level and waste reduction.
# Applied on top of base.yaml.
policy:
  mode: balanced
  safety_stock_multiplier: 1.0
  forecast_quantile: 0.55
  max_cover_days: null           # cover full shelf life minus 1 day buffer
  round_up_shipment: true
